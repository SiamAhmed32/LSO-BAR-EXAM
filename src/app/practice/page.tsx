import { Layout } from '@/components'
import PracticePage from '@/components/PracticePageSection/PracticePage'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <Layout>
        <PracticePage/>
    </Layout>
  )
}

export default page