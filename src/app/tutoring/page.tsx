import { Layout } from '@/components'
import TutoringPage from '@/components/TutoringPageSection/TutoringPage'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <Layout>
        <TutoringPage/>
    </Layout>
  )
}

export default page

